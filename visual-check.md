# OmniTutor Visual Verification

## Desktop — 1280×720

The RTL header, brand lockup, navigation, hero content, learning-level selector, quota card, and chat empty state render with clear hierarchy. The teal-and-sand palette, rounded cards, soft shadows, and responsive two-column layout match the intended elegant learning workspace.

## Mobile — 375×812

The header condenses into a menu icon and the main layout stacks vertically. The hero wraps without clipping, the level selector remains easy to tap, and the chat panel begins below the fold with comfortable spacing. Arabic text remains readable and the page preserves RTL direction.

## Automated checks

TypeScript check passed. Vitest passed with 4 test files and 7 tests. The project includes tests for auth logout, quota calculations, database-backed plan routing, media status, and early chat validation.
